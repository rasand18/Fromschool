object whatIsArgs {
  // Huvudprogram:
  def main(args : Array[String]) : Unit = {
    /** Nu följer en loop
     * som loopar igenom
     * alla argument
     */
    for(a<-args) {
      println(a)
    }
  }
}
